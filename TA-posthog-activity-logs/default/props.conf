[posthog:activity_log]
# The modular input sets each event's time explicitly from OCSF `time`, so leave it alone.
# Setting DATETIME_CONFIG = CURRENT here would override that with ingest time and break
# every time-based search.
SHOULD_LINEMERGE = false
KV_MODE = json
TRUNCATE = 0
