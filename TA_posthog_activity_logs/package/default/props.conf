[posthog:activity_log]
# The input sets each event's time explicitly from OCSF `time`. Do not add DATETIME_CONFIG here:
# it would override that with ingest time and break every time-based search.
SHOULD_LINEMERGE = false
KV_MODE = json
TRUNCATE = 0
