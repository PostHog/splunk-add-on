# ucc-gen build artifacts. The built package bundles platform-specific binaries from the
# machine that ran the build, so releases are built in CI rather than committed.
output/
*.tar.gz
*.spl
__pycache__/
*.pyc
.DS_Store
